<p id="notice"><%= notice %></p>

<div id="tabs"> 
  <ul>
    <li><%= link_to 'All Requests', requests_path%></li>
    <li><%= link_to "Requests I've Posted",  _employee_requests_path %></li>
    <li><%= link_to 'My Profile', employee_path(current_employee) %></li>     
<!-- do check to see if admin to show dashboard tab on home tab position views -->
    <li id = 'selected'><%= link_to 'Dashboard Reports', dashboards_path%></li>
  </ul>
</div>

<div id="inner_innercontainer">

<div id="tabs"> 
  <ul>
    <li><%= link_to 'All Requests Table', dashboards_path %></li>
    <li><%= link_to 'Requests Summary', 'requests_reports' %></li>
    <li><%= link_to "Skills Summary", 'skills_reports'  %></li>
    <li id="selected"><%= link_to 'OneTeam Impact', 'impact_reports'  %></li>     
  </ul>
</div>

<div id="innercontainer">

<h3>How many different offices have developers worked for in the last six months?</h3>
<table>
  <tr>
    <th>Developer</th>
    <th><%= Time.now.months_ago(5).strftime('%b') %></th>
    <th><%= Time.now.months_ago(4).strftime('%b') %></th>
    <th><%= Time.now.months_ago(3).strftime('%b') %></th>
    <th><%= Time.now.months_ago(2).strftime('%b') %></th>
    <th><%= Time.now.months_ago(1).strftime('%b') %></th>
    <th><%= Time.now.strftime('%b') %></th>
  </tr>
<% @dashboard.offices_impact.each do |office| %>
  <tr>
    <td><%= office["fname"] %> <%= office["lname"] %></td>
    <td><%= office["month1"] %></td>
    <td><%= office["month2"] %></td>
    <td><%= office["month3"] %></td>
    <td><%= office["month4"] %></td>
    <td><%= office["month5"] %></td>
    <td><%= office["month6"] %></td>
<% end %>
  </tr>
</table>

<h3>How many outside developers worked at each office in the last six months?</h3>
<table>
  <tr>
    <th>Date</th>
    <th>Chicago</th>
    <th>Mumbai</th>
    <th>Houston</th>
    <th>Boston</th>
    <th>San Francisco</th>
    <th>London</th>
  </tr>
<% @dashboard.six_month_developers_impact.each do |office| %>
  <tr>
    <td><%= office["date"] %></td>
    <td><%= office["chicago"] %></td>
    <td><%= office["mumbai"] %></td>
    <td><%= office["houston"] %></td>
    <td><%= office["boston"] %></td>
    <td><%= office["sanfran"] %></td>
    <td><%= office["london"] %></td>
<% end %>
  </tr>
</table>

<br />

</div>
</div>
